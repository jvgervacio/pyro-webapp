import {MainTemplate} from '../components/template';

const LayoutPage: React.FC = () => {
  return (
    <MainTemplate className='' title='LAYOUT'>
      
    </MainTemplate>
  );
};

export default LayoutPage;