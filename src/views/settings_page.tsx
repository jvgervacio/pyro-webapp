import {MainTemplate} from '@components/template';

const SettingsPage: React.FC = () => {

  return (
    <MainTemplate className='' title='SETTINGS'>

    </MainTemplate>
  );
};

export default SettingsPage;